require "spec_helper"

RSpec.describe Evolis::PremiumSdk do
  it "has a version number" do
    expect(Evolis::PremiumSdk::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
